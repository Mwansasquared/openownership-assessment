# Native PostgreSQL (Homebrew / system install) — no password needed
DATABASE_URL=postgres://localhost/openownership?sslmode=disable

# Docker-based PostgreSQL
# DATABASE_URL=postgres://dev:dev@localhost:5432/openownership?sslmode=disable

JWT_SECRET=change-me-to-a-long-random-string
PORT=8080
FRONTEND_ORIGIN=http://localhost:5173
